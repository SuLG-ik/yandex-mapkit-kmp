---
title: "YMKDrivingLandmark"
kind: "Enum"
platform: "ios"
language: "Objective-C"
section: "iOS / Справочник / Objective-C / Enums / YMKDrivingLandmark"
source: "https://yandex.ru/maps-api/docs/mapkit/Objective-C/YMKDrivingLandmark.html"
---
# YMKDrivingLandmark

`IOS` · `Objective-C` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Objective-C/YMKDrivingLandmark.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```objectivec
enum YMKDrivingLandmark : NSUInteger {}
```

Information about an upcoming landmark.

## Summary

### Enum cases

```objectivec
YMKDrivingLandmarkUnknown
```

Unknown landmark

```objectivec
YMKDrivingLandmarkAtTrafficLights
```

Traffic lights

```objectivec
YMKDrivingLandmarkBeforeTrafficLights
```

The area before traffic lights

```objectivec
YMKDrivingLandmarkBeforeBridge
```

The area before a bridge

```objectivec
YMKDrivingLandmarkBeforeTunnel
```

The area before a tunnel

```objectivec
YMKDrivingLandmarkAfterBridge
```

The area after a bridge

```objectivec
YMKDrivingLandmarkAfterTunnel
```

The area after a tunnel

```objectivec
YMKDrivingLandmarkToBridge
```

The area leading to a bridge

```objectivec
YMKDrivingLandmarkIntoTunnel
```

The area leading to a tunnel

```objectivec
YMKDrivingLandmarkIntoCourtyard
```

The turn into a courtyard

```objectivec
YMKDrivingLandmarkToFrontageRoad
```

The area leading to a frontage road

## Enum cases

### YMKDrivingLandmarkUnknown

```objectivec
YMKDrivingLandmarkUnknown
```

Unknown landmark.

### YMKDrivingLandmarkAtTrafficLights

```objectivec
YMKDrivingLandmarkAtTrafficLights
```

Traffic lights.

### YMKDrivingLandmarkBeforeTrafficLights

```objectivec
YMKDrivingLandmarkBeforeTrafficLights
```

The area before traffic lights.

### YMKDrivingLandmarkBeforeBridge

```objectivec
YMKDrivingLandmarkBeforeBridge
```

The area before a bridge.

### YMKDrivingLandmarkBeforeTunnel

```objectivec
YMKDrivingLandmarkBeforeTunnel
```

The area before a tunnel.

### YMKDrivingLandmarkAfterBridge

```objectivec
YMKDrivingLandmarkAfterBridge
```

The area after a bridge.

### YMKDrivingLandmarkAfterTunnel

```objectivec
YMKDrivingLandmarkAfterTunnel
```

The area after a tunnel.

### YMKDrivingLandmarkToBridge

```objectivec
YMKDrivingLandmarkToBridge
```

The area leading to a bridge.

### YMKDrivingLandmarkIntoTunnel

```objectivec
YMKDrivingLandmarkIntoTunnel
```

The area leading to a tunnel.

### YMKDrivingLandmarkIntoCourtyard

```objectivec
YMKDrivingLandmarkIntoCourtyard
```

The turn into a courtyard.

### YMKDrivingLandmarkToFrontageRoad

```objectivec
YMKDrivingLandmarkToFrontageRoad
```

The area leading to a frontage road.
