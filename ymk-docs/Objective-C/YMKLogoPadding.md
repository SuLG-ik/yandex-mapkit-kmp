---
title: "YMKLogoPadding"
kind: "Class"
platform: "ios"
language: "Objective-C"
section: "iOS / Справочник / Objective-C / Classes / YMKLogoPadding"
source: "https://yandex.ru/maps-api/docs/mapkit/Objective-C/YMKLogoPadding.html"
---
# YMKLogoPadding

`IOS` · `Objective-C` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Objective-C/YMKLogoPadding.html)

```objectivec
@interface YMKLogoPadding : NSObject
```

Logo padding class.

## Summary

### Class methods

```objectivec
+ (nonnull YMKLogoPadding *)paddingWithHorizontalPadding:( NSUInteger)horizontalPadding
                                         verticalPadding:( NSUInteger)verticalPadding;
```

### Properties

```objectivec
@property (nonatomic, readonly) NSUInteger horizontalPadding;
```

Defines horizontal padding

```objectivec
@property (nonatomic, readonly) NSUInteger verticalPadding;
```

Defines vertical padding

## Class methods

### paddingWithHorizontalPadding:verticalPadding:

```objectivec
+ (nonnull YMKLogoPadding *)paddingWithHorizontalPadding:( NSUInteger)horizontalPadding
                                         verticalPadding:( NSUInteger)verticalPadding;
```

## Properties

### horizontalPadding

```objectivec
@property (nonatomic, readonly) NSUInteger horizontalPadding;
```

Defines horizontal padding.

### verticalPadding

```objectivec
@property (nonatomic, readonly) NSUInteger verticalPadding;
```

Defines vertical padding.
