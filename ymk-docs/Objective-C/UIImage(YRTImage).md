---
title: "UIImage(YRTImage)"
kind: "Category"
platform: "ios"
language: "Objective-C"
section: "iOS / Справочник / Objective-C / Categories / UIImage(YRTImage)"
source: "https://yandex.ru/maps-api/docs/mapkit/Objective-C/UIImage(YRTImage).html"
---
# UIImage(YRTImage)

`IOS` · `Objective-C` · `Category` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Objective-C/UIImage(YRTImage).html)

```objectivec
@interface UIImage (YRTImage)
```

## Summary

### Properties

```objectivec
@property (nonatomic, assign, unsafe_unretained, readwrite) BOOL yrtCacheable;
```

Determines whether image should be cached on GPU or not

## Properties

### yrtCacheable

```objectivec
@property (nonatomic, assign, unsafe_unretained, readwrite) BOOL yrtCacheable;
```

Determines whether image should be cached on GPU or not.
