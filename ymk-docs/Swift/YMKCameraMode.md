---
title: "YMKCameraMode"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKCameraMode"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKCameraMode.html"
---
# YMKCameraMode

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKCameraMode.html)

> [!WARNING] Важно
> This feature is available in the NaviKit SDK version.

```swift
enum YMKCameraMode : UInt, @unchecked Sendable
```

## Summary

### Enum cases

```swift
case following = 0
```

Camera follow position

```swift
case free = 1
```

Free camera mode

```swift
case overview = 2
```

Camera showing all routes

```swift
case indoor = 3
```

Indoor mode

## Enum cases

### following

```swift
case following = 0
```

Camera follow position.

### free

```swift
case free = 1
```

Free camera mode.

### overview

```swift
case overview = 2
```

Camera showing all routes.

### indoor

```swift
case indoor = 3
```

Indoor mode. It is supported only in transport navigation
