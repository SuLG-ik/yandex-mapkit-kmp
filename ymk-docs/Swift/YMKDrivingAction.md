---
title: "YMKDrivingAction"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKDrivingAction"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingAction.html"
---
# YMKDrivingAction

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingAction.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
enum YMKDrivingAction : UInt, @unchecked Sendable
```

Driver actions.

## Summary

### Enum cases

```swift
case straight = 0
```

Going straight

```swift
case slightLeft = 1
```

Going slightly to the left

```swift
case slightRight = 2
```

Going slightly to the right

```swift
case left = 3
```

Going left

```swift
case right = 4
```

Going right

```swift
case hardLeft = 5
```

Turning left

```swift
case hardRight = 6
```

Turning right

```swift
case forkLeft = 7
```

Choosing a fork to the left

```swift
case forkRight = 8
```

Choosing a fork to the right

```swift
case uturnLeft = 9
```

U-turn to the left

```swift
case uturnRight = 10
```

U-turn to the right

```swift
case enterRoundabout = 11
```

Entering a roundabout

```swift
case leaveRoundabout = 12
```

Leaving a roundabout

```swift
case boardFerry = 13
```

Boarding a ferry

```swift
case leaveFerry = 14
```

Leaving a ferry

```swift
case exitLeft = 15
```

Exiting to the left

```swift
case exitRight = 16
```

Exiting to the right

```swift
case finish = 17
```

Finishing the route

```swift
case waypoint = 18
```

Passing waypoint

## Enum cases

### straight

```swift
case straight = 0
```

Going straight.

### slightLeft

```swift
case slightLeft = 1
```

Going slightly to the left.

### slightRight

```swift
case slightRight = 2
```

Going slightly to the right.

### left

```swift
case left = 3
```

Going left.

### right

```swift
case right = 4
```

Going right.

### hardLeft

```swift
case hardLeft = 5
```

Turning left.

### hardRight

```swift
case hardRight = 6
```

Turning right.

### forkLeft

```swift
case forkLeft = 7
```

Choosing a fork to the left.

### forkRight

```swift
case forkRight = 8
```

Choosing a fork to the right.

### uturnLeft

```swift
case uturnLeft = 9
```

U-turn to the left.

### uturnRight

```swift
case uturnRight = 10
```

U-turn to the right.

### enterRoundabout

```swift
case enterRoundabout = 11
```

Entering a roundabout.

### leaveRoundabout

```swift
case leaveRoundabout = 12
```

Leaving a roundabout.

### boardFerry

```swift
case boardFerry = 13
```

Boarding a ferry.

### leaveFerry

```swift
case leaveFerry = 14
```

Leaving a ferry.

### exitLeft

```swift
case exitLeft = 15
```

Exiting to the left.

### exitRight

```swift
case exitRight = 16
```

Exiting to the right.

### finish

```swift
case finish = 17
```

Finishing the route.

### waypoint

```swift
case waypoint = 18
```

Passing waypoint
