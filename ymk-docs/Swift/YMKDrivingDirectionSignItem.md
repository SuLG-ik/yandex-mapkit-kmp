---
title: "YMKDrivingDirectionSignItem"
kind: "Class"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Classes / YMKDrivingDirectionSignItem"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingDirectionSignItem.html"
---
# YMKDrivingDirectionSignItem

`IOS` · `Swift` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingDirectionSignItem.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
class YMKDrivingDirectionSignItem : NSObject
```

## Summary

### Class methods

```swift
 init(toponym: YMKDrivingDirectionSignToponym)
```

```swift
 init(road: YMKDrivingDirectionSignRoad)
```

```swift
 init(exit: YMKDrivingDirectionSignExit)
```

```swift
 init(icon: YMKDrivingDirectionSignIcon)
```

### Properties

```swift
var toponym: YMKDrivingDirectionSignToponym? { get }
```

```swift
var road: YMKDrivingDirectionSignRoad? { get }
```

```swift
var exit: YMKDrivingDirectionSignExit? { get }
```

```swift
var icon: YMKDrivingDirectionSignIcon? { get }
```

## Class methods

### init(toponym:)

```swift
 init(toponym: YMKDrivingDirectionSignToponym)
```

### init(road:)

```swift
 init(road: YMKDrivingDirectionSignRoad)
```

### init(exit:)

```swift
 init(exit: YMKDrivingDirectionSignExit)
```

### init(icon:)

```swift
 init(icon: YMKDrivingDirectionSignIcon)
```

## Properties

### toponym

```swift
var toponym: YMKDrivingDirectionSignToponym? { get }
```

### road

```swift
var road: YMKDrivingDirectionSignRoad? { get }
```

### exit

```swift
var exit: YMKDrivingDirectionSignExit? { get }
```

### icon

```swift
var icon: YMKDrivingDirectionSignIcon? { get }
```
