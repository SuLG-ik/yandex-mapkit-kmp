---
title: "YMKDrivingLandmark"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKDrivingLandmark"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingLandmark.html"
---
# YMKDrivingLandmark

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingLandmark.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
enum YMKDrivingLandmark : UInt, @unchecked Sendable
```

Information about an upcoming landmark.

## Summary

### Enum cases

```swift
case unknown = 0
```

Unknown landmark

```swift
case atTrafficLights = 1
```

Traffic lights

```swift
case beforeTrafficLights = 2
```

The area before traffic lights

```swift
case beforeBridge = 3
```

The area before a bridge

```swift
case beforeTunnel = 4
```

The area before a tunnel

```swift
case afterBridge = 5
```

The area after a bridge

```swift
case afterTunnel = 6
```

The area after a tunnel

```swift
case toBridge = 7
```

The area leading to a bridge

```swift
case intoTunnel = 8
```

The area leading to a tunnel

```swift
case intoCourtyard = 9
```

The turn into a courtyard

```swift
case toFrontageRoad = 10
```

The area leading to a frontage road

## Enum cases

### unknown

```swift
case unknown = 0
```

Unknown landmark.

### atTrafficLights

```swift
case atTrafficLights = 1
```

Traffic lights.

### beforeTrafficLights

```swift
case beforeTrafficLights = 2
```

The area before traffic lights.

### beforeBridge

```swift
case beforeBridge = 3
```

The area before a bridge.

### beforeTunnel

```swift
case beforeTunnel = 4
```

The area before a tunnel.

### afterBridge

```swift
case afterBridge = 5
```

The area after a bridge.

### afterTunnel

```swift
case afterTunnel = 6
```

The area after a tunnel.

### toBridge

```swift
case toBridge = 7
```

The area leading to a bridge.

### intoTunnel

```swift
case intoTunnel = 8
```

The area leading to a tunnel.

### intoCourtyard

```swift
case intoCourtyard = 9
```

The turn into a courtyard.

### toFrontageRoad

```swift
case toFrontageRoad = 10
```

The area leading to a frontage road.
