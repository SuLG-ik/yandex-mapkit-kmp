---
title: "YMKAnimationType"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKAnimationType"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnimationType.html"
---
# YMKAnimationType

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnimationType.html)

```swift
enum YMKAnimationType : UInt, @unchecked Sendable
```

Available animation types.

## Summary

### Enum cases

```swift
case smooth = 0
```

Smooth interpolation between start and finish states

```swift
case linear = 1
```

Movement with constant speed during animation time

## Enum cases

### smooth

```swift
case smooth = 0
```

Smooth interpolation between start and finish states.

### linear

```swift
case linear = 1
```

Movement with constant speed during animation time.
