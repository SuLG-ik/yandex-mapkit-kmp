---
title: "YMKAnnotatedRoadEvents"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKAnnotatedRoadEvents"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnnotatedRoadEvents.html"
---
# YMKAnnotatedRoadEvents

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnnotatedRoadEvents.html)

> [!WARNING] Важно
> This feature is available in the NaviKit SDK version.

```swift
struct YMKAnnotatedRoadEvents : OptionSet, @unchecked Sendable
```

## Summary

### Enum cases

```swift
static var danger: YMKAnnotatedRoadEvents { get }
```

```swift
static var reconstruction: YMKAnnotatedRoadEvents { get }
```

```swift
static var accident: YMKAnnotatedRoadEvents { get }
```

```swift
static var school: YMKAnnotatedRoadEvents { get }
```

```swift
static var overtakingDanger: YMKAnnotatedRoadEvents { get }
```

```swift
static var pedestrianDanger: YMKAnnotatedRoadEvents { get }
```

```swift
static var crossRoadDanger: YMKAnnotatedRoadEvents { get }
```

```swift
static var laneControl: YMKAnnotatedRoadEvents { get }
```

Traffic code control

```swift
static var roadMarkingControl: YMKAnnotatedRoadEvents { get }
```

```swift
static var crossRoadControl: YMKAnnotatedRoadEvents { get }
```

```swift
static var mobileControl: YMKAnnotatedRoadEvents { get }
```

```swift
static var speedLimitControl: YMKAnnotatedRoadEvents { get }
```

```swift
static var trafficControl: YMKAnnotatedRoadEvents { get }
```

```swift
static var policePatrol: YMKAnnotatedRoadEvents { get }
```

```swift
static var trafficControls: YMKAnnotatedRoadEvents { get }
```

```swift
static var everything: YMKAnnotatedRoadEvents { get }
```

## Enum cases

### danger

```swift
static var danger: YMKAnnotatedRoadEvents { get }
```

### reconstruction

```swift
static var reconstruction: YMKAnnotatedRoadEvents { get }
```

### accident

```swift
static var accident: YMKAnnotatedRoadEvents { get }
```

### school

```swift
static var school: YMKAnnotatedRoadEvents { get }
```

### overtakingDanger

```swift
static var overtakingDanger: YMKAnnotatedRoadEvents { get }
```

### pedestrianDanger

```swift
static var pedestrianDanger: YMKAnnotatedRoadEvents { get }
```

### crossRoadDanger

```swift
static var crossRoadDanger: YMKAnnotatedRoadEvents { get }
```

### laneControl

```swift
static var laneControl: YMKAnnotatedRoadEvents { get }
```

Traffic code control

### roadMarkingControl

```swift
static var roadMarkingControl: YMKAnnotatedRoadEvents { get }
```

### crossRoadControl

```swift
static var crossRoadControl: YMKAnnotatedRoadEvents { get }
```

### mobileControl

```swift
static var mobileControl: YMKAnnotatedRoadEvents { get }
```

### speedLimitControl

```swift
static var speedLimitControl: YMKAnnotatedRoadEvents { get }
```

### trafficControl

```swift
static var trafficControl: YMKAnnotatedRoadEvents { get }
```

### policePatrol

```swift
static var policePatrol: YMKAnnotatedRoadEvents { get }
```

### trafficControls

```swift
static var trafficControls: YMKAnnotatedRoadEvents { get }
```

### everything

```swift
static var everything: YMKAnnotatedRoadEvents { get }
```
