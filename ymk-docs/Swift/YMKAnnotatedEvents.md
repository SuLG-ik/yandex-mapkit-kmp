---
title: "YMKAnnotatedEvents"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKAnnotatedEvents"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnnotatedEvents.html"
---
# YMKAnnotatedEvents

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKAnnotatedEvents.html)

> [!WARNING] Важно
> This feature is available in the NaviKit SDK version.

```swift
struct YMKAnnotatedEvents : OptionSet, @unchecked Sendable
```

## Summary

### Enum cases

```swift
static var manoeuvres: YMKAnnotatedEvents { get }
```

```swift
static var fasterAlternative: YMKAnnotatedEvents { get }
```

```swift
static var roadEvents: YMKAnnotatedEvents { get }
```

```swift
static var tollRoadAhead: YMKAnnotatedEvents { get }
```

```swift
static var speedLimitExceeded: YMKAnnotatedEvents { get }
```

```swift
static var parkingRoutes: YMKAnnotatedEvents { get }
```

```swift
static var streets: YMKAnnotatedEvents { get }
```

```swift
static var routeStatus: YMKAnnotatedEvents { get }
```

```swift
static var wayPoints: YMKAnnotatedEvents { get }
```

```swift
static var speedBumps: YMKAnnotatedEvents { get }
```

```swift
static var railwayCrossings: YMKAnnotatedEvents { get }
```

```swift
static var lanes: YMKAnnotatedEvents { get }
```

```swift
static var routeActions: YMKAnnotatedEvents { get }
```

```swift
static var everything: YMKAnnotatedEvents { get }
```

## Enum cases

### manoeuvres

```swift
static var manoeuvres: YMKAnnotatedEvents { get }
```

### fasterAlternative

```swift
static var fasterAlternative: YMKAnnotatedEvents { get }
```

### roadEvents

```swift
static var roadEvents: YMKAnnotatedEvents { get }
```

### tollRoadAhead

```swift
static var tollRoadAhead: YMKAnnotatedEvents { get }
```

### speedLimitExceeded

```swift
static var speedLimitExceeded: YMKAnnotatedEvents { get }
```

### parkingRoutes

```swift
static var parkingRoutes: YMKAnnotatedEvents { get }
```

### streets

```swift
static var streets: YMKAnnotatedEvents { get }
```

### routeStatus

```swift
static var routeStatus: YMKAnnotatedEvents { get }
```

### wayPoints

```swift
static var wayPoints: YMKAnnotatedEvents { get }
```

### speedBumps

```swift
static var speedBumps: YMKAnnotatedEvents { get }
```

### railwayCrossings

```swift
static var railwayCrossings: YMKAnnotatedEvents { get }
```

### lanes

```swift
static var lanes: YMKAnnotatedEvents { get }
```

### routeActions

```swift
static var routeActions: YMKAnnotatedEvents { get }
```

### everything

```swift
static var everything: YMKAnnotatedEvents { get }
```
