---
title: "YMKRequestPointType"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKRequestPointType"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKRequestPointType.html"
---
# YMKRequestPointType

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKRequestPointType.html)

```swift
enum YMKRequestPointType : UInt, @unchecked Sendable
```

The waypoint and a point the path must go through.

## Summary

### Enum cases

```swift
case waypoint = 0
```

The target waypoint

```swift
case viapoint = 1
```

A point the route must pass through

## Enum cases

### waypoint

```swift
case waypoint = 0
```

The target waypoint.

### viapoint

```swift
case viapoint = 1
```

A point the route must pass through.
