---
title: "YMKSpannableString"
kind: "Class"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Classes / YMKSpannableString"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKSpannableString.html"
---
# YMKSpannableString

`IOS` · `Swift` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKSpannableString.html)

```swift
class YMKSpannableString : NSObject
```

A string that supports spans in it.

## Summary

### Class methods

```swift
 init(text: String, spans: [YMKSpannableStringSpan])
```

### Properties

```swift
var text: String { get }
```

The text of the spannable string

```swift
var spans: [YMKSpannableStringSpan] { get }
```

The spans in stored text

## Class methods

### init(text:spans:)

```swift
 init(text: String, spans: [YMKSpannableStringSpan])
```

## Properties

### text

```swift
var text: String { get }
```

The text of the spannable string.

### spans

```swift
var spans: [YMKSpannableStringSpan] { get }
```

The spans in stored text.
