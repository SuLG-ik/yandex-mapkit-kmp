---
title: "YMKTileId"
kind: "Class"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Classes / YMKTileId"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKTileId.html"
---
# YMKTileId

`IOS` · `Swift` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKTileId.html)

```swift
class YMKTileId : NSObject
```

The ID of a tile on the map.

## Summary

### Class methods

```swift
 init(x: UInt, y: UInt, z: UInt)
```

### Properties

```swift
var x: UInt { get }
```

The number of the tile horizontally

```swift
var y: UInt { get }
```

The number of the tile vertically

```swift
var z: UInt { get }
```

The number of columns and rows to split the map into

## Class methods

### init(x:y:z:)

```swift
 init(x: UInt, y: UInt, z: UInt)
```

## Properties

### x

```swift
var x: UInt { get }
```

The number of the tile horizontally.

### y

```swift
var y: UInt { get }
```

The number of the tile vertically.

### z

```swift
var z: UInt { get }
```

The number of columns and rows to split the map into.
