---
title: "YMKDrivingVehicleType"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKDrivingVehicleType"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingVehicleType.html"
---
# YMKDrivingVehicleType

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingVehicleType.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
enum YMKDrivingVehicleType : UInt, @unchecked Sendable
```

The type of the vehicle that is used for the route.

## Summary

### Enum cases

```swift
case `default` = 0
```

Standard vehicle type

```swift
case taxi = 1
```

Allowed only for Yandex apps

```swift
case truck = 2
```

```swift
case moto = 3
```

## Enum cases

### default

```swift
case `default` = 0
```

Standard vehicle type.

### taxi

```swift
case taxi = 1
```

Allowed only for Yandex apps

### truck

```swift
case truck = 2
```

### moto

```swift
case moto = 3
```
