---
title: "YMKDrivingActionMetadata"
kind: "Class"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Classes / YMKDrivingActionMetadata"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingActionMetadata.html"
---
# YMKDrivingActionMetadata

`IOS` · `Swift` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingActionMetadata.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
class YMKDrivingActionMetadata : NSObject
```

## Summary

### Class methods

```swift
 init(uturnMetadata: YMKDrivingUturnMetadata)
```

```swift
 init(leaveRoundaboutMetadata: YMKDrivingLeaveRoundaboutMetadata)
```

```swift
 init(turnMetadata: YMKDrivingTurnMetadata)
```

```swift
 init(exitMetadata: YMKDrivingExitMetadata)
```

### Properties

```swift
var uturnMetadata: YMKDrivingUturnMetadata? { get }
```

```swift
var leaveRoundaboutMetadata: YMKDrivingLeaveRoundaboutMetadata? { get }
```

```swift
var turnMetadata: YMKDrivingTurnMetadata? { get }
```

```swift
var exitMetadata: YMKDrivingExitMetadata? { get }
```

## Class methods

### init(uturnMetadata:)

```swift
 init(uturnMetadata: YMKDrivingUturnMetadata)
```

### init(leaveRoundaboutMetadata:)

```swift
 init(leaveRoundaboutMetadata: YMKDrivingLeaveRoundaboutMetadata)
```

### init(turnMetadata:)

```swift
 init(turnMetadata: YMKDrivingTurnMetadata)
```

### init(exitMetadata:)

```swift
 init(exitMetadata: YMKDrivingExitMetadata)
```

## Properties

### uturnMetadata

```swift
var uturnMetadata: YMKDrivingUturnMetadata? { get }
```

### leaveRoundaboutMetadata

```swift
var leaveRoundaboutMetadata: YMKDrivingLeaveRoundaboutMetadata? { get }
```

### turnMetadata

```swift
var turnMetadata: YMKDrivingTurnMetadata? { get }
```

### exitMetadata

```swift
var exitMetadata: YMKDrivingExitMetadata? { get }
```
