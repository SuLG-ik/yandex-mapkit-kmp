---
title: "YMKDrivingLaneKind"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKDrivingLaneKind"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingLaneKind.html"
---
# YMKDrivingLaneKind

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKDrivingLaneKind.html)

> [!WARNING] Важно
> This feature is available in the Full MapKit SDK version

```swift
enum YMKDrivingLaneKind : UInt, @unchecked Sendable
```

The type of lane.

## Summary

### Enum cases

```swift
case unknownKind = 0
```

Unknown lane type

```swift
case plainLane = 1
```

Common use lane

```swift
case busLane = 2
```

A lane for buses

```swift
case tramLane = 3
```

A lane for trams

```swift
case taxiLane = 4
```

A lane for taxis

```swift
case bikeLane = 5
```

A lane for bikes

## Enum cases

### unknownKind

```swift
case unknownKind = 0
```

Unknown lane type.

### plainLane

```swift
case plainLane = 1
```

Common use lane.

### busLane

```swift
case busLane = 2
```

A lane for buses.

### tramLane

```swift
case tramLane = 3
```

A lane for trams.

### taxiLane

```swift
case taxiLane = 4
```

A lane for taxis.

### bikeLane

```swift
case bikeLane = 5
```

A lane for bikes.
