---
title: "YMKLocationUseInBackground"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKLocationUseInBackground"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKLocationUseInBackground.html"
---
# YMKLocationUseInBackground

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKLocationUseInBackground.html)

```swift
enum YMKLocationUseInBackground : UInt, @unchecked Sendable
```

If UseInBackground is Allow then the 'location' flag must be set in 'UIBackgroundModes' of the application.

## Summary

### Enum cases

```swift
case allow = 0
```

```swift
case disallow = 1
```

## Enum cases

### allow

```swift
case allow = 0
```

### disallow

```swift
case disallow = 1
```
