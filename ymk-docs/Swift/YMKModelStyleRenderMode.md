---
title: "YMKModelStyleRenderMode"
kind: "Enum"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Enums / YMKModelStyleRenderMode"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKModelStyleRenderMode.html"
---
# YMKModelStyleRenderMode

`IOS` · `Swift` · `Enum` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKModelStyleRenderMode.html)

```swift
enum YMKModelStyleRenderMode : UInt, @unchecked Sendable
```

## Summary

### Enum cases

```swift
case building = 0
```

Model should be rendered with buildings from ground layer

```swift
case userModel = 1
```

Model should be rendered within separate sublayer

## Enum cases

### building

```swift
case building = 0
```

Model should be rendered with buildings from ground layer.

### userModel

```swift
case userModel = 1
```

Model should be rendered within separate sublayer.
