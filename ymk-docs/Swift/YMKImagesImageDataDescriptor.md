---
title: "YMKImagesImageDataDescriptor"
kind: "Class"
platform: "ios"
language: "Swift"
section: "iOS / Справочник / Swift / Classes / YMKImagesImageDataDescriptor"
source: "https://yandex.ru/maps-api/docs/mapkit/Swift/YMKImagesImageDataDescriptor.html"
---
# YMKImagesImageDataDescriptor

`IOS` · `Swift` · `Class` — [оригинал](https://yandex.ru/maps-api/docs/mapkit/Swift/YMKImagesImageDataDescriptor.html)

```swift
class YMKImagesImageDataDescriptor : NSObject
```

## Summary

### Class methods

```swift
 init(imageId: String,
 primaryColor: UIColor?,
 secondaryColor: UIColor?,
 tertiaryColor: UIColor?)
```

### Properties

```swift
var imageId: String { get }
```

```swift
var primaryColor: UIColor? { get }
```

Optional field, can be nil

```swift
var secondaryColor: UIColor? { get }
```

Optional field, can be nil

```swift
var tertiaryColor: UIColor? { get }
```

Optional field, can be nil

## Class methods

### init(imageId:primaryColor:secondaryColor:tertiaryColor:)

```swift
 init(imageId: String,
 primaryColor: UIColor?,
 secondaryColor: UIColor?,
 tertiaryColor: UIColor?)
```

## Properties

### imageId

```swift
var imageId: String { get }
```

### primaryColor

```swift
var primaryColor: UIColor? { get }
```

Optional field, can be nil.

### secondaryColor

```swift
var secondaryColor: UIColor? { get }
```

Optional field, can be nil.

### tertiaryColor

```swift
var tertiaryColor: UIColor? { get }
```

Optional field, can be nil.
